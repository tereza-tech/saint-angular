import { Observable } from "rxjs";

interface UsersState {
  activeUser: string | null;
  users: User[];
}

interface users {
  users: User[]
}

interface User {
  id: string;
}



class alfa {
  John: User = { "id": "aR2C" }
  Mike: User = { "id": "Aup9" };

  /*Alive: UsersState =
  Guys: users =
  Joe: User =
  Boys: users =
  */
}


